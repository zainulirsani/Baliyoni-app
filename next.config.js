/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['127.0.0.1', 'localhost'], // Tambahkan domain IP kamu di sini
  },
  reactStrictMode: true,
}

module.exports = nextConfig
